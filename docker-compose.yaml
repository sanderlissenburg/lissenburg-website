version: '3.7'

services:
  traefik:
    image: traefik:v2.0
    command: --api.insecure=true --providers.docker --providers.docker.exposedByDefault=false
    ports:
      - '80:80'
      - '8080:8080'
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - traefik

  mysql:
    container_name: mysql
    image: mysql:8.0.19
    command: --default-authentication-plugin=mysql_native_password
    environment:
      - MYSQL_ROOT_PASSWORD=$DB_PASSWORD
    ports:
      - 3306:3306
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - mysql

  client:
    image: nginx:1.17.10-alpine
    volumes:
      - ./client/public:/usr/share/nginx/html:ro
    labels:
      - traefik.http.routers.lissenburg_client.rule=Host(`lissenburg.test`)
      - traefik.enable=true
    networks:
      - traefik

  admin_nginx:
    build:
      context: ./admin
      dockerfile: ./docker/nginx/Dockerfile
    volumes:
      - ./admin/public:/app/public:ro
    networks:
      - traefik
      - admin
    labels:
      - traefik.http.routers.lissenburg_admin.rule=Host(`lissenburg.test`) && PathPrefix(`/admin`)
      - traefik.enable=true

  admin_php-fpm:
    container_name: php-fpm
    build:
      context: ./admin
      dockerfile: ./docker/php-fpm/Dockerfile.dev
    volumes:
      - ./admin:/app
    networks:
      - admin
      - mysql

networks:
  traefik:
    name: traefik
    driver: bridge
  mysql:
    name: mysql
    driver: bridge
  admin:
    name: admin
    driver: bridge

volumes:
  mysql_data:
    name: mysql_data
